Test Steps	Expected Results	Test Data / Notes
"1. Open the generated batch file.
2. Locate the first record and confirm it starts with the File Header record type code (FH).
3. Validate File Header field values (file creation date/time, version, sequence) against the source system."	"1. File opens without corruption.
2. First record's type code matches the configured File Header prefix.
3. All File Header field values match the expected values and lengths per the layout spec."	Layout ref: FH rule in layout-config.csv.
"1. Locate the Batch Header (BH) record in the settlement file.
2. Validate Merchant ID, Batch Number and Batch Date fields.
3. Confirm the Batch Header sequence number field is present and numeric."	"1. Exactly one BH record exists per batch.
2. Values match the terminal/merchant configuration for the batch.
3. Sequence number field is populated and numeric, position/length per spec."	Batch Header sequence number is expected to change between runs and must be excluded from any cross-file comparison.
"1. Close batch N and capture its Batch Header sequence number.
2. Open and close batch N+1.
3. Compare the sequence numbers of batch N and batch N+1."	"1. Sequence number recorded.
2. Batch N+1 settlement file generated.
3. Sequence number for batch N+1 is incremented per the terminal's numbering rule (no reuse, no gap unless expected)."	
"1. Extract the RRN (Retrieval Reference Number) from every Transaction Detail (TXV) record in the batch.
2. Check for duplicate RRN values within the batch."	"1. All RRNs extracted.
2. No duplicate RRNs exist within a single settlement file, except where a legitimate reversal/duplicate scenario is explicitly under test (see TC-EXC-006)."	RRN position confirmed at file position 122, length 12 in the current sample layout - verify against real spec.
"1. Count the number of Transaction Detail (TXV) records in the settlement file.
2. Read the record count field in the Batch Trailer (BT) record."	"1. Count recorded, e.g. N.
2. BT record count field equals N."	
"1. Sum the transaction amount field across every TXV record in the batch (approved transactions only).
2. Read the total amount field in the Batch Trailer (BT) record."	"1. Total calculated, e.g. X.
2. BT total amount equals X, accounting for correct decimal/implied-decimal handling."	
"1. Locate the last record in the settlement file.
2. Validate File Trailer field values (e.g. total record count for the file) against the actual file content."	"1. Last record's type code matches the configured File Trailer prefix (FT).
2. File Trailer values match actual file content."	
"1. Trigger batch close with zero transactions in the batch.
2. Inspect the resulting settlement file."	"1. Batch closes without application error or crash.
2. File contains FH, BH (record count = 0), BT (record count = 0, total amount = 0), and FT records; no TXV records present."	
"1. Attempt to trigger batch close/settlement again for the same, already-settled batch.
2. Confirm no duplicate settlement file is transmitted to the host/acquirer."	"1. System rejects the duplicate settlement attempt with a clear error/warning.
2. Only one settlement file exists for the batch; no duplicate transmission occurs."	
"1. For each record type, measure the total line length.
2. For each declared field within a record, extract the substring at its documented start position and length."	"1. Line length matches the spec's expected length for that record type.
2. Extracted value is well-formed for its data type (numeric fields contain digits, date fields are valid dates, etc.) and does not bleed into the neighboring field."	Use the TSYS EDC file comparator tool's layout-config.csv as the source of truth for positions once confirmed against the real spec.
"1. Initiate a Sale transaction for a valid amount using a valid card.
2. Inspect the transaction record written to the batch.
3. Confirm the transaction is included in the next settlement."	"1. Transaction is authorized online by the issuer.
2. Record contains correct amount, transaction date/time, and authorization code.
3. Transaction appears in the settlement file's TXV records and contributes to the Batch Trailer totals."	
"1. Initiate a Refund transaction against the original Sale (same RRN/reference where supported).
2. Inspect the resulting transaction record.
3. Confirm the refund is reflected as a negative/credit amount in the next settlement totals."	"1. Refund is authorized/processed.
2. Record correctly identifies it as a refund and references the original transaction where the format supports it.
3. Batch Trailer total amount correctly nets the refund against sales."	
"1. Initiate a Void against the un-settled Sale transaction.
2. Confirm the batch settlement no longer includes the voided transaction (or includes it net-zero, per implementation)."	"1. Void is accepted.
2. Voided transaction does not contribute to the final settlement total."	
"1. Initiate a Pre-Authorization for amount A.
2. Initiate a Completion for a different amount B (e.g. final bill with incidentals).
3. Inspect the settled transaction record."	"1. Pre-Auth approved and hold placed for amount A.
2. Completion approved for amount B.
3. Settlement amount equals B, not A."	
"1. Initiate a Sale for an amount greater than the available balance on a partial-approval-capable card.
2. Inspect the transaction record and any prompted remaining-balance flow."	"1. Issuer responds with a partial approval for a lesser amount.
2. Transaction record reflects the actually approved (partial) amount, not the originally requested amount."	
"1. Initiate a Sale that will be declined by the issuer.
2. Check the next settlement file."	"1. Transaction is declined; decline reason is displayed/logged.
2. No TXV record exists for the declined transaction, and it does not contribute to Batch Trailer totals."	
"1. Complete a Sale for a base amount.
2. Apply a tip adjustment within the configured maximum percentage.
3. Inspect the settled amount."	"1. Sale approved.
2. Adjustment accepted.
3. Settled amount equals base amount plus the applied tip."	
"1. Complete a Sale for a base amount.
2. Attempt a tip adjustment exceeding the configured maximum percentage."	"1. Sale approved.
2. System rejects the adjustment or caps it at the configured maximum; it does not silently accept an over-limit tip."	
1. Attempt a Refund referencing the original Sale for an amount greater than the original Sale amount.	1. System rejects the over-amount refund attempt with a clear error.	
1. Attempt a second Refund referencing the same original Sale transaction.	1. System rejects the duplicate refund attempt with a clear error; no second refund record is created.	
"1. Insert a CVN10 test chip card and perform an online Sale.
2. Capture the ARQC-related data elements (transaction data, UDK/session key derivation inputs)."	"1. Terminal generates an ARQC and sends it in the authorization request (DE55).
2. Terminal-generated ARQC matches the value independently computed by the HSM/reference tool for the same input data."	Depends on correct UDK derivation and session key generation per CVN10 rules.
"1. Insert a CVN18 test chip card and perform an online Sale.
2. Capture and independently verify the ARQC."	"1. Terminal generates an ARQC using CVN18 rules and sends it in the authorization request.
2. Terminal-generated ARQC matches the independently computed reference value."	Confirm correct AC data format assumptions - a prior investigation flagged a non-standard HSM AC data format for CVN18; retest against the resolved format.
"1. Perform an online chip Sale that receives an approval with ARPC in the response.
2. Observe terminal's issuer-script/ARPC validation step."	"1. Terminal receives the ARPC.
2. Terminal validates ARPC successfully and completes the transaction without a cryptogram-validation error."	
"1. Perform a chip transaction under conditions configured to breach an issuer risk parameter (e.g. floor limit, velocity check).
2. Confirm the transaction is not sent for online authorization and is not included in settlement."	"1. Card responds with an AAC (Application Authentication Cryptogram) - offline decline.
2. Terminal correctly ends the transaction as declined; no TXV settlement record is created."	
"1. Insert the multi-application card.
2. Observe application selection (auto or via cardholder prompt, per terminal configuration)."	"1. Terminal builds the candidate application list from the card.
2. Terminal selects the AID matching its configured priority/mutual support with the card, per EMV application selection rules."	
"1. Insert the card and initiate a Sale above any no-CVM-required floor limit.
2. Enter the correct online/offline PIN.
3. Enter an incorrect PIN."	"1. Terminal prompts for PIN entry.
2. PIN is verified (online PIN via issuer, or offline PIN via card) and transaction proceeds.
3. Transaction is declined / PIN retry is offered per configured PIN try limit."	
"1. Insert the chip-fault test card.
2. Allow the terminal to prompt for magstripe fallback (where scheme rules permit)."	"1. Terminal detects chip read failure after the configured number of attempts.
2. Terminal processes the transaction as a magstripe fallback, tagging the transaction accordingly for the host and settlement record."	Confirm fallback is only offered where scheme/issuer rules allow it - some schemes disable fallback in certain regions.
1. Tap the contactless card for an amount under the CVM limit.	1. Transaction is approved without a PIN or signature prompt.	
1. Tap the contactless card for an amount over the CVM limit.	1. Terminal either prompts for PIN or requests the card be inserted for a contact chip transaction, per its configuration.	
"1. Capture the authorization request message for the transaction.
2. Decode the DE55 TLV structure and validate mandatory EMV tags (9F02 amount, 9F26 ARQC, 9F10 IAD, 9F37 unpredictable number, 9F1A terminal country code, etc.)."	"1. DE55 (or equivalent field) is present and TLV-encoded.
2. All mandatory tags are present, correctly TLV-encoded, and contain plausible values for the transaction."	
"1. Initiate a Sale and simulate the host not responding within the configured timeout window.
2. Observe terminal behavior after timeout."	"1. Terminal times out waiting for the authorization response.
2. Terminal automatically generates and sends a reversal message for the timed-out transaction; the transaction is not settled as approved."	
"1. Initiate a Sale and simulate power loss after the authorization request is sent but before a response is received/processed.
2. Power the terminal back on."	"1. Terminal powers off mid-transaction.
2. Terminal detects the incomplete transaction on restart and performs recovery (status check / reversal) per its recovery logic - it does not resubmit the original request or create a duplicate settlement record."	
"1. Insert the chip card and begin a Sale.
2. Remove the card before processing completes."	"1. Transaction processing begins.
2. Terminal detects the removal, safely aborts the transaction, and displays an appropriate error; no partial/corrupt settlement record is created."	
"1. Close a batch and simulate a communication failure during file transmission.
2. Observe terminal/system retry behavior."	"1. Initial transmission attempt fails.
2. System retries transmission per its configured retry/backoff policy and eventually succeeds (or raises a clear, actionable alert after exhausting retries) without corrupting or duplicating the settlement file."	
"1. Run file validation/comparison against the batch containing the malformed record.
2. Confirm the remaining valid records are still processed and reported correctly."	"1. Malformed record is identified and reported as an anomaly (e.g. flagged as UNKNOWN / failing field validation).
2. All other valid records in the batch are still parsed, matched, and compared correctly - one bad record does not abort the whole comparison/validation run."	Directly exercises the TSYS EDC file comparator's 'unmapped record type still surfaces in the report' behavior.
"1. Resubmit the same transaction with the same RRN (simulating a terminal retry after an ambiguous response).
2. Check the resulting batch."	"1. Host/terminal duplicate-detection logic identifies the resubmission.
2. The transaction is not double-counted in the settlement totals or reported as two separate legitimate transactions with the same RRN (see TC-BATCH-004 and TC-CMP-006)."	
"1. Run the file comparator against the production and test files.
2. Inspect the Content Differences result for the reordered records."	"1. Comparison completes without error.
2. The reordered records are matched correctly by their key (e.g. RRN) and are NOT reported as missing, extra, or differing - only their row position changed."	Confirms the core 'different row is not a mismatch' requirement.
"1. Run the comparator against the production and test files.
2. Inspect the Missing Records section."	"1. Comparison completes.
2. The removed record's key, type, and production line number are correctly reported as MISSING_IN_TEST."	
"1. Run the comparator against the production and test files.
2. Inspect the Extra Records section."	"1. Comparison completes.
2. The extra record's key, type, and test line number are correctly reported as EXTRA_IN_TEST."	
"1. Run the comparator against the production and test files.
2. Inspect the Content Differences entry for the altered record."	"1. Comparison completes.
2. Reported start position and length exactly match where the injected change was made, and the reported production/test values match the actual injected values."	
"1. Run the comparator against the production and test files.
2. Inspect the overall result and the Batch Header record's comparison outcome."	"1. Comparison completes.
2. No difference is reported for the Batch Header despite the sequence number differing; overall result is PASS if nothing else differs."	Validates the configured ignore zone (position 3, length 4) is honored.
"1. Run the comparator against a file pair where one file contains the duplicate RRN.
2. Inspect the Duplicate Key Warnings section."	"1. Comparison completes.
2. The duplicate key, its record type, and both line numbers are reported as a warning, and matching still proceeds sensibly for the remaining records."	
"1. Run the comparator against the two files.
2. Inspect the overall result field."	"1. Comparison completes.
2. Result is PASS; MissingCount, ExtraCount, and DifferenceCount are all zero."	
"1. Run the comparator against the two large files and record execution time.
2. Verify the known injected differences are all correctly detected."	"1. Comparison completes within an acceptable time threshold (define per environment, e.g. under N seconds) and without memory errors.
2. All injected missing/extra/content differences are reported; no false positives are introduced by the larger record count."	Validates the key-based HashMap matching design does not degrade to quadratic behavior at scale.
"1. Process transactions under two different Merchant IDs on the same terminal within one business day.
2. Inspect the settlement file(s) generated at close-of-day."	"1. Transactions processed and recorded.
2. Transactions are correctly grouped under their respective Merchant ID's Batch Header, with independent batch totals per merchant."	
"1. Process transactions on two different Terminal IDs under the same Merchant ID.
2. Inspect the settlement output."	"1. Transactions processed and recorded.
2. Terminal ID is correctly preserved per transaction record, and merchant-level totals correctly consolidate across both terminals per the host/acquirer's expected reconciliation model."	
"1. Re-run the full regression suite (all suites above) against the upgraded terminal.
2. Pay particular attention to Batch File & Settlement and EMV Chip Processing suites, where format or cryptogram logic changes are most likely to regress."	"1. All previously passing test cases continue to pass.
2. No new failures are introduced in file layout, cryptogram generation/validation, or settlement totals as a result of the upgrade."	This case anchors the whole suite as the release regression gate - link it to the release work item.
